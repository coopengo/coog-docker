#!/bin/bash
#----------
# Shortcut to docker-compose up after initialisation of env vars and services
#----------
BASE_PATH=`realpath "$(dirname $0)/.."`
. "${BASE_PATH}/bin/helpers/colors.sh"

${BASE_PATH}/bin/init

section "Starting up"
docker-compose --env-file $BASE_PATH/.env up $@
