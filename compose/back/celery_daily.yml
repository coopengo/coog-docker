services:
  celery-daily:
    extends:
      file: compose/back/common.yml
      service: back-common
    command: ["daily", "4", "french_work_days"]
    env_file:
      - env/back.env
      - env/celery-daily.env
    restart: "no"
    deploy:
      restart_policy:
        condition: none
