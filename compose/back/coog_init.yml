services:
  init:
    extends:
      file: compose/back/common.yml
      service: back-common
    command: ["admin", "-cu", "ir"]
    restart: on-failure
    deploy:
      restart_policy:
        condition: on-failure
