services:
  celery:
    extends:
      file: compose/back/common.yml
      service: back-common
    command: ["celery", "${COOG_CELERY_WORKERS:?}", "${COOG_CELERY_CUSTOM_QUEUES}"]
    env_file:
      - env/back.env
      - env/celery.env
    restart: always
